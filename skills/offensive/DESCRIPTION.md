---
description: "Offensive-security skills for authorized engagements (pentest, CTF, bug-bounty, red-team). Dispatched through the `terminal` tool against a pinned Kali Linux image (scarlight_constants.DEFAULT_TERMINAL_IMAGE). Tools install on demand via apt — common surface: nmap, gobuster, sqlmap, nikto, dig, whois, curl, openssl. Engagements are opt-in: with an active engagement.yaml each skill re-checks the target against its scope; with none declared the session runs unscoped (CTF / lab / ad-hoc) and skills proceed without demanding a scope file. The operator is bound by CODE_OF_USE.md in either mode."
---
