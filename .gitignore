.DS_Store
/venv/
/_pycache/
*.pyc*
__pycache__/
.venv/
.vscode/
.claude/
# Operator-local engagement scope files + lab scratch space — never committed.
# Sanitized demo scopes under demo/ are committed (see !demo/engagement.yaml).
lab/
engagement.yaml
!demo/engagement.yaml
# Local presenter script for live demos — kept out of the repo.
demo/PITCH.md
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.development
.env.test
export*
__pycache__/model_tools.cpython-310.pyc
__pycache__/web_tools.cpython-310.pyc
logs/
data/
.pytest_cache/
tmp/
temp_vision_images/
scarlight-*/*
tests/quick_test_dataset.jsonl
tests/sample_dataset.jsonl
run_datagen_kimik2-thinking.sh
run_datagen_megascience_glm4-6.sh
run_datagen_sonnet.sh
source-data/*
run_datagen_megascience_glm4-6.sh
data/*
node_modules/
browser-use/
agent-browser/
# Private keys
*.ppk
*.pem
privvy*
images/
__pycache__/
scarlight.egg-info/
wandb/
out/
testlogs

# CLI config (may contain sensitive SSH paths)
cli-config.yaml

# Skills Hub state (lives in ~/.scarlight/skills/.hub/ at runtime, but just in case)
skills/.hub/
ignored/
.worktrees/
environments/benchmarks/evals/

# Web UI build output
scarlight_cli/web_dist/

# Web UI assets — synced from @nous-research/ui at build time via
# `npm run sync-assets` (see web/package.json).
web/public/fonts/
web/public/ds-assets/

# Release script temp files
.release_notes.md
mini-swe-agent/

# Nix
.direnv/
.nix-stamps/
result
website/static/api/skills-index.json
models-dev-upstream/

# Operator-console integration bridges — console (private/paid) code that the
# console's Rust host runs from $SCARLIGHT_ROOT at runtime. Canonical copies live
# in the scarlight-console repo (bridges/). Must NOT enter OSS public history.
query_bridge.py
recon_bridge.py
connectors_bridge.py

# semideus-cognition local agent worktrees / scratch — never committed.
.semideus/
